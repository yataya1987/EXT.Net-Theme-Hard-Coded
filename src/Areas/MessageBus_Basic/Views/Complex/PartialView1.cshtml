@{
    var X = Html.X();
}

@(X.Panel()
    .Title("PartialView1")
    .Flex(1)
    .Height(200)
    .Layout(LayoutType.Fit)
    .MessageBusListeners(new MessageBusListener
    {
        Name = "ToPartialView1.*",
        Handler = "this.body.createChild({html: name, tag:'p'});"
    })
    .Buttons(b => {
        b.Add(X.Button()
            .Text("Publish 'FromPartialView.FromPV1'")
            .Listeners(l => l.Click.BroadcastOnBus = "FromPartialView.FromPV1")
        );

        b.Add(X.Button()
            .Text("Publish 'ToPartialView2.event1'")
            .Listeners(l => l.Click.BroadcastOnBus = "ToPartialView2.event1")
        );
    })

)